# Dummy app.py to satisfy Hugging Face Space requirements if it expects an app_file
# For Docker SDK, this file is ignored, but it prevents "missing configuration" errors if the space was created with Gradio.
print("This file is a placeholder. The actual backend runs via Dockerfile executing FastAPI.")
